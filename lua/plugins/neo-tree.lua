return {
  -- "nvim-neo-tree/neo-tree.nvim",
  -- opts = {
  --   filesystem = {
  --     filtered_items = {
  --       visible = true,           -- This is the magic key: it shows filtered items but dims them
  --       hide_dotfiles = true,     -- Keep this true so they are "filtered" (and thus dimmed)
  --       hide_gitignored = true,   -- This will also dim git-ignored files
  --     },
  --   },
  -- },
}
